<%= link_to "メッセージ作成へ", new_message_path %>
<p>
<% @messages.each do |message| %>
<%= message.title %>
<%= link_to "詳細", message_path(message.id) %>
<%= link_to "編集", edit_message_path(message.id) %>
</p>
<% end %>
